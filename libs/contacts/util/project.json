{
  "name": "contacts-util",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/contacts/util/src",
  "prefix": "app",
  "projectType": "library",
  "tags": ["scope:contacts", "type:util"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/contacts/util"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
